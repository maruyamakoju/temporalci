FROM python:3.11-slim

WORKDIR /app
COPY . /app

RUN pip install --no-cache-dir --upgrade pip && \
    pip install --no-cache-dir -e .[distributed]

EXPOSE 8080
CMD ["temporalci-distributed", "serve", "--host", "0.0.0.0", "--port", "8080"]
